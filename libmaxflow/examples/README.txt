The files gc_pixel.cpp, gc_pixel.cpp illustrate how to use libmaxflow together with libift.
Both applications are capable of binary segmentation given markers (written by iftWriteSeeds2D) that are either 0 (BG) or 1 (FG).

See the Makefile for compiling and linking details.